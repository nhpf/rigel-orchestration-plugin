# Goal
int64[] input_numbers

---
# Result
# A string array with each entry containing the factorization of one number
# e.g. "60 = 2 x 2 x 3 x 5"
string[] factorization

---
# Feedback
# Shows the current number being factored, and each prime factor found
int64 current_number
int64 factor
